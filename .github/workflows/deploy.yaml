name: Push images to docker registry
on:
  push:
    branches:
      - main # check your repo, your default branch might be master!
 
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: docker login
        run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - name: build docker images
        run: |
          docker build -t $DOCKER_USER/multi-react -f Dockerfile ./fibo-react
          docker build -t $DOCKER_USER/multi-server -f Dockerfile ./server
          docker build -t $DOCKER_USER/multi-worker -f Dockerfile ./worker
          docker build -t $DOCKER_USER/multi-nginx -f Dockerfil ./nginx
      - name: push docker images
        run: |
          docker push $DOCKER_USER/multi-react
          docker push $DOCKER_USER/multi-server
          docker push $DOCKER_USER/multi-worker
          docker push $DOCKER_USER/multi-nginx
